function CalculationAssumptionForm(forecast, tmc, salesPrice, inflow,
    outflow, packSize, calculation, share, initialOutlay) {
    this.forecast = forecast;
    this.tmc = tmc;
    this.salesPrice = salesPrice;
    this.inflow = inflow;
    this.outflow = outflow;
    this.packSize = packSize;
    this.calculation = calculation;
    this.share = share;
    this.initialOutlay = initialOutlay;
}

